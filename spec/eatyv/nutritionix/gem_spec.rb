RSpec.describe Eatyv::Nutritionix::Gem do
  it "has a version number" do
    expect(Eatyv::Nutritionix::Gem::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
